# ROLA-IP Site

Static ISP proxy marketing site built with Vue 3, Vite, TypeScript, GSAP, and Tailwind CSS.

## Development

```bash
npm install
npm run dev
```

The dev server listens on `0.0.0.0` so the site can be previewed from other devices on the same network.

## Production Build

```bash
npm run build
```
